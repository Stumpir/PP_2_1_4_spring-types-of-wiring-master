package koschei.models;

import org.springframework.stereotype.Component;

@Component
public class Deth8 { // Вывод не очень красивый получается, потому что у последнего объекта Deth8 в строчке
}                    // не переопределен toString в исходном проекте, но так как в задании написано только
                     // создать связь до 8 элемента, то оставил так
